//交换头尾，数组中每个元素都要挪动一位，循环m次。 想法来自插入排序（桌上m张牌，每次拿最底下的放到手牌左边），步骤很像插入排序
//如果用vecotr<int>而不是数组的话，可以只挪动首尾元素（末元素插到A.begin()，然后pop末元素），但既然题目说了数组。。。

#include<iostream>
using namespace std;
int main()
{
	int n, m;
	cin >> n >> m;
	int A[100];
	for (int i = 0;i < n;++i)cin >> A[i];
	while(m--){
		int temp = A[n - 1];
		for (int i = n-1;i > 0;--i) A[i] = A[i-1];
		A[0] = temp;
	}
	cout << A[0];
	for (int i = 1;i < n;++i)cout <<" "<<A[i];
}
